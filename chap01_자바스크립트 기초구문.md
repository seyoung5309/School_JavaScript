# JavaScript 기초 문법

---

# 자바스크립트 환경 설정

1. 마이크로소프트에서 제공하는 개발 IDE인 비주얼 스튜디오 공식사이트트(https://code.visualstudio.com)에서 다운로드 후 설치 (바탕화면 만들기, path추가)
2. Node.js의 공식 사이트(https://nodejs.org)에 접속해서 Node.js을 다운로드 받아서 설치
3. vs 실행 후 terminal 에서
   - node -v 실행(버전 확인)

   - npm -v 실행(버전 확인)

   -에러 발생시??

4. vs 에 유용한 도구 설치

- html css suppport, HTML snippets(geyao가 제공=> 상황에 따라 선택), JavaScript (ES6) code snippets, ESLint, Prettier- Code formatter(이후 두 가지 설정), live server

- Prettier-Code formatter 설정 : 좌측 하단에 톱니바퀴 모양 선택 -> setting -> default formatter -> Prettier-Code formatter 로 설정 , format on save 를 체크

---

# 1. 자바스크립트란

- **동적인 웹 사이트 제작을 위해 사용**됨

1. 역사 : 1990년대 넷스케이프의 브랜던 아이크(Brenden Eich)가 Mocha라는 이름으로 출시하고, 라이브스크립트로 개발하였다가 최종적으로 자바스크립트가 됨. 그러나 브라우져(넷스케이프, 익스플로러....)에 따라 상이한 스크립트가 나오기 시작하면서 개발이 쉽지 않았으며(if 구문의 남발...).... 스크립트의 표준화에 대한 요구가 생기기 시작함.

2. ECMA(European Computer Manufactures Association) 스크립트 : 자바스크립트의 표준작업이 시작된 것으로 1997년 6월 정식으로 채택된 스크립트의 표준. 모던 스크립트는 ES(ECMA script의 약자)6 버전에서 시작한다고 본다.

3. 바닐라 자바스크립트(바닐라 JS) : 어떠한 라이브러리(JQeury..)나 프레임워크(React, Vue, Angular....)를 사용하지 않는 순수 자바스크립트, 가장 빠르고 가벼운 크로스 플랫폼 프레임워크 => 실력 향상을 위한 필수 단계 => 결코 쉽지 않다...=> 그러나 프레임워크나 라이브러리는 모두 바닐라 자바스크립트로 만들어져 있다. => 자신의 실력과 가치를 높이는 강력한 수단이 될 수 있다.

4. 바닐라 자바스크립트 시작하기

- 사이트 : http://vanilla-js.com/
- 시작 : <script src="path/to/vanilla.js"></script>를 html 문서안에 삽입
- 위트 있는 사이트 ^^ : 다운받아도 아무 것도 없다!!!!
- 순수 자바스크립트로 짜면 좋다!!!!

- 최근에는 자바스크립트를 이용하여 웹뿐만 아니라 서버(NodeJS), 데스크톱 프로그램(Electron)도 제작하고 있는 추세
- 최근 [가장 인기있는 언어](http://githut.info/)로 높은 점유율을 차지하고 있음

---

# 2. 자바스크립트 작성 위치

- 외부 파일에서 불러오기 (권장) : ` <script src="./js/01_js_location.js"></script>`
- 파일 안 script 태그에서 직접 사용하기 (수업에서 자주 사용) => `<body>` 중 어디가 가장 좋을까?
- 인라인 자바스크립트 사용
- **CSS의 사용 방식**(외부, style 태그 사용, 인라인 스타일)과 비슷
- 실습 (01_js_location.html)

---

# 3. console 객체의 log 함수

- console에 대한 내장함수를 사용 가능
- 부라우져의 개발자 도구 (f12)를 통해 확인 가능
- 실습 (02_console.html)

---

# 4. 변수 선언자 var, let, const

- 변수란? 프로그램 실행에 필요한 데이터를 담는 기억공간
  - 장점: 데이터 타입에 따른 선언(int, float, double...)이 필요하지 않다.
  - 단점: 데이터 타입에 관계없는 변수이기 때문에 개발 시 오류 발생이 가능

- 변수 선언자 종류::var,(ES6부터)let, const

> #### ★필기 _(byte, char, int, long, short, double)_ _(javascript에서 prompt()는 string만 입력받는다.)_

- 선언 형식: 선언자 변수명 = 값:

- 변수 작성 규칙(4가지) :
  1.  카멜 표기법(camelCase) : 두 가지 이상의 단어가 합쳐질 경우, 두 번째부터 단어의 첫글자를 대문자로.
      `var userName = "Seyoung";`

  2.  스네이크 표기법(snake_case) : 두 개 이상의 단어가 합쳐질 경우 단어 사이에 삽입
      `var user_name = "Seyoung;`

  3.  파스칼 표기법(PascalCase) : 모든 단어의 첫 글자를 대문자
      `var UserName = "Seyoung;`

  4.  헝가리안 표기법 : 변수명 맨 앞에 데이터의 타입에 해당하는 영어 단어를 추가하고, 나머지는 카멜 표기법을 따른다.
      `var strUserName = "Seyoung";`

- **var** 선언자의 특징
  - ES6 이전부터 사용한 선언자
  - 재선언(동일한 이름으로 재선언이 가능), 재할당 가능
  - 재선언이 가능한 변수 => 협업을 하면서 동일한 이름의 변수를 var로 선언할 경우 덮어쓰기가 가능해서 예상하지 않은 결과가 나올 수 있다.
  - 전역 변수로 사용 가능

- **let** 선언자의 특징
  - ES6 이후부터 사용한 선언자
  - 재선언이 불가능한 변수 선언 => 동일한 변수명을 다른 곳에서 사용 불가 => 권장
  - 재할당은 가능

- **const** 선언자의 특징
  - ES6 이후부터 사용한 선언자
  - 변하지 않는 상수값 선언에 사용
  - 상수는 대문자로 표기한다.
  - 재선언, 재할당 모두 불가능하다.(재할당을 해도 에러가 발생하지 않고 예외가 발생한다.)
  - 협업하는 ㅐ발자들이 통일해서 사용하는 데이터 선언 등에 사용

- 실습(03_variable_declaration.html)

---

# 5. 기본 데이터 타입

1. 문자열(String) 객체

- 기호: "" 또는 '로 데이터를 할당. 가급적 한가지로 통일하는 것이 좋다.
- 속성 및 함수: 문자열 데이터. 멤버(속성(property, attribute)(파란색) 또는 함수(method)(보라색))
- 코딩 규칙: 팀별 코딩 규칙에 따라 문자열 " 또는 '로 지정한다.
- 코딩 중 에러 발생 주의
  - `let str1 = "너희는 '2학년 1반'이야."`
  - `let str2 = '너희는 "2학년 1반"이야';`
  - `let str3 = "너희는 \"2학년 1반\"이야."; // escape 문자`
- 문자열 합치기: +
- 실습(04_dataType.html)

2. 숫자(Number)형 객체

- 특징: 정수 또는 실수 모든 숫자
- 속성 및 함수
- 64비트 부동소수점 표현의 문제점(정밀도 오차): 자바스크립트는 자바(int, long, float, double...)등과 달리 메모리는 64비트 부동소수점 형태로 모든 숫자를 관리 => 문제(약간의 오차)가 발생할 수 있다.
- 정밀도 오차의 해결 방법
  - 정수로 변환 후 계산
  - 출력 시점에 toFixed() 사용
- 실습(04_dataType.html)

3. Boolean 객체

- 특징
- 실습(04_dataType.html)

4. undefined 객체

- 특징
  - 정의되지 않은, 값을 할당하지 않은 객체
  - 데이터 타입이자 데이터 값이다.
  - 실행되는 시점, 즉 런타임에 브라우저 엔진이 자동으로 undefied 라는 값을 할당
  - 개발자의 의도보다는 실수인 경우가 많다.
- 실습(04_dataType.html)

5. null 객체

- 특징
  - 없음을 의미하는 데이터 타입
  - 실수가 아닌 개발자가 임의로 설정한 경우가 많다.
  - 선언 시점에는 어떤 값이나 어떤 데이터 타입이 올지 정해지지 않은 경우
  - Garbage Collection (가비지 컬렉션: 더 이상 사용하지 않는 데이터를 메모리에서 삭제하는 기능)
- 실습(04_dataType.html)

6. symbol 객체 - _(실무에서 사용 거의 안함)_

- 특징

---

# 6. 데이터 타입 - Object

- 정의 : 관련 있는 데이터를 하나로 담을 수 있는 객체 _(python에서 iterable 객체 - 딕셔너리(Dictionary))_
- 사용법 : {}를 사용
- 특징 : 관리가 매우 쉽다.

1. 관련 있는 정보를 하나로 묶어서 관리하고 싶은 경우 사용 : 사람 - 이름, 전화번호, 이메일 주소, 집 주소 등을 묶고 싶은 경우

2. 키(key : name, phone, email 등 ....) 와 값(value)를 쌍으로 갖는다.

3. 객체의 값에 접근하는 방법(.연산자 사용) : 이름.키

4. 객체의 값 변경 : 객체명.키 = 값

5. 객체에 새로운 키와 값을 추가 : 객체명.키 = 값

6. object 의 특정 키에 접근하는 방법 : 객체 이름 ["키"]

- 실습(05_Object.html)

---

# 7. 데이터 타입 - Array

1. 배열의 정의

- 하나 이상의 데이터를 담을 수 있다.
- 데이터의 순서가 있다. => 데이터에 접근하고 수정, 편집이 가능하다.
- []를 이용해서 할당
- 어떤 데이터 타입도 담을 수 있다.
- 어떤 데이터 타입도 함께 담을 수 있다. let mix = [1, "a", 3, true, {}, []];

2. 배열의 순서

- 순번 : 인덱스(index)
- 인덱스는 0부터 1씩 증가한다.
- 인덱스를 이요하여 데이터에 접근 및 수정이 가능하다.

3. 배열의 활용

- 다양한 내장함수(push(), find(), concat()...)를 사용할 수 있다.
- object와 함께 활용이 가능(배열 안에 object가 있는 형태)

- 실습(06_Array.html)

---

# 8. 64비트 부동소수점

1. 에러를 발생시켜 보자.
2. 에러의 발생을 미연에 방지하려면?
3. 자바스크립트에서 사용하는 가장 큰 수와 작은 수는?

- 실습

---

# 9. 연산자

1. 할당 연산자

- 특징 : 오른쪽 피연산자의 값을 왼쪽 변수에 할당
- 종류 : 복합 활당 연산자(+=, -=, \*=, /=, %/)
- 실습(08_operator.html)

2. 비교 연산자

- 특징 : 값(타입) 비교
- 종류 : (==, ===,!=, !===, >, >=, <, <=)
- 값과 타입의 비교 : ===
- 실습(08_operator.html)

3. 산술 연산자

- 특징 : 증가, 감소, 단항, 부정(-), 숫자화(+)
- 종류 : +, -, \*, /, %, \*\*(제곱근), ++, --
- 실습(08_operator.html)

3-1. 단항 부정 연산자

- 특징 : 부호가 바뀜
- 실습(08_operator.html)

3-2. 숫자화 연산자

- 특징 : 숫자가 아닌 문자(boolean) 값을 숫자로 변환 시도
- 실습(08_operator.html)

4. 논리 연산자

- 특징 : 조건을 여러 가지 만족시키는지 알아보는 연산자
- 종류 : &&, ||
- 실습(08_operator.html)

5. 문자열 관련 연산자

- 특징 : 합치기
- 종류 : +
- 실습(08_operator.html)

6. 조건(삼항) 연산자

- 특징 :
  - 조건 ? true일 때 할당할 값 : false 일 때 할당할 값
  - 중첩해서 사용이 가능하다.
- 실습(08_operator.html)

---

# 10. 조건문

- 특징
  - 가장 중요한 구문 중 하나이다.
  - 프로그램 = 조건문의 집합
  - if: 만일 ~ 라면
  - 작성 시 순서가 효율성에 영향을 미친다.

1. 조건문 if

- if : 조건을 만족할 경우 실행할 코드 블록
- if else ...: 조건을 만족할 경우 실행할 코드 블록
- else: 조건을 만족하지 않을 경우 실행할 코드 블록
- 항상 false 인 값 :
- 실습(09_if.html)

2. 조건문 switch

- 특징
- 장점
- 단점
- 실습(10_switch.html)

- 참고: **Date**객체

---

# 11. 반복문

- 특징

1. for loop

- 특징
- 동작 원리
- 실습(11_for_loop.html)

- for 과 break
- for 과 continue

1-1. for ~ in

- 특징
- 실습(12_for_in.html)

1-2. for ~ of

- 특징
- 실습(13_for_of.html)

1-3. for ~ each

- 특징
- 실습(14_for_each.html)

2.  while loop

- 특징
- 실습(15_while.html)

3.  do - while

- 특징
- 실습

4. 반복문의 활용

---

# 12. 함수

- 정의
- 특징

1. 함수 선언식 방식

- 선언
- 사용
- 실습(16_function.html)
- common.js 파일의 활용 및 장점

2. 함수 표현식 방식

- 선언
- 사용
- 유의할 사항
- 실습(16_function.html)

3. Function 생성자 함수

- 선언
- 사용
- 언제 유용하게 사용할까?
- 실습(16_function.html)

4. 화살표 함수

5. 함수 자체가 파라미터로 전달되는 함수

- forEach() 실습
