/**
 * 환율 계산?
 * @param {*} productPrice
 * @returns
 */
function taxAmount(productPrice) {
  let tax = 0.1;
  return productPrice * tax;
}

/**
 * 한국 부자
 * @returns
 */
function top5() {
  return ["a", "b", "c", "d"];
}
